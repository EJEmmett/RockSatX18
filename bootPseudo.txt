//speudo code for boot for both of the cubesat pi's

import os
import time from timer
//this code imports the modules like time and operating system

declare string bootFile
set bootfile = "destination/destination2/boot.txt"
bootFile version = 1
open file to be written in

print time in bootFileResults.txt
sleep //so that they don't print on top of eachother
run ping other pi print results in boot.txt

send results to home reciver
//the code above will be more then one line 

end code
